# 架构设计

本应用采用零依赖 Node 服务和浏览器前端组成，目标是让目录扫描、全文检索和文档切换保持轻量。

## 服务端

- 扫描 `docs/` 目录下所有 `.md` 文件。
- 建立标题、路径、正文、标签和关键词索引。
- 根据文档链接、标签重合、关键词相似度生成知识树图谱。

## 前端

- 左侧展示文件夹目录和搜索结果。
- 中央提供阅读、修改、预览和图谱视图。
- Canvas 图谱使用轻量力导向布局，适合中小型技术知识库。

相关文档：[[搜索设计]]

#architecture #search #graph
