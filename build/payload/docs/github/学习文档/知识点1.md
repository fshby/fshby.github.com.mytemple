## 一、将项目推送到 GitHub

### 1. 创建 `.gitignore`（在 `qrcode-replacer/` 目录下）


![screenshot-1783697311591](/source/screenshot-1783697311591.webp)


```
node_modules
dist
.DS_Store
*.log
.vscode/*
!.vscode/extensions.json
.idea
.env
.env.local
```

### 2. 初始化 Git 并首次推送

在 `e:\trae\QR code` 或 `e:\trae\QR code\qrcode-replacer` 中执行：

```powershell
# 1) 初始化仓库
cd "e:\trae\QR code"
git init
git add .
git commit -m "chore: initial commit"
