{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "MyTemple Knowledge 默认能力：允许主窗口加载本地 axum 服务并调用 shell + dialog 插件",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:webview:default",
    "core:window:default",
    "shell:allow-open",
    "dialog:default"
  ]
}
